import Config

# Production: set these in runtime.exs or the release env — never commit real secrets.
config :lunity, :player_ws_token, nil
config :lunity, :player_jwt_secret, nil
config :lunity, :player_mint_secret, nil
